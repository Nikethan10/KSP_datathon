# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── Heavy regenerable artifacts ───────────────────────────────────────
# feature_matrix.parquet alone is 134 MB, over GitHub's 100 MB hard limit
cache/

# Pipeline outputs (~134 MB / 690 files). Regenerate with `python main.py`
# (~22 min). The frontend does NOT read these directly — copy_data.py
# copies what the UI needs into frontend/public/data, which IS committed.
outputs/

# ── Build output + Catalyst staging ──────────────────────────────────
frontend/dist/
client/assets/
client/data/
client/brand/
client/index.html
client/favicon.svg
client/icons.svg

# Stale Vite scaffold from `catalyst init` — not the real frontend
react-app/

# ── Node ─────────────────────────────────────────────────────────────
frontend/node_modules/
node_modules/
*.log

# ── IDE / editor config ───────────────────────────────────────────────
.claude/
frontend/.claude/
.vscode/
.idea/

# ── Submission / presentation docs ───────────────────────────────────
*.pdf
*.pptx
*.docx

# ── One-off utility scripts ───────────────────────────────────────────
make_ppt.py
fix_commits.py
_msg_filter.py
make_brand_assets.py
precompute_calibration.py
precompute_district_trends.py
precompute_novelty.py
precompute_offenders.py
precompute_patrol_all.py
precompute_patrol_scenarios.py
precompute_socioeconomic.py
rebuild_benchmark.py
rerun_fairness.py
rerun_local_hotspots.py
rerun_patrol.py

# ── Test files ────────────────────────────────────────────────────────
test_skeleton.py

# ── Merged into README ────────────────────────────────────────────────
STATUS.md
DEPLOY.md
CONTRIBUTIONS.md

# ── Source media (large originals; processed versions in frontend/public/brand) ──
prahari_loadingscreen.mp4
prahari_logo.jpg
scratch_preview.png

# ── OS ───────────────────────────────────────────────────────────────
Thumbs.db
.DS_Store
desktop.ini
*.tmp
