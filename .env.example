# Absolute path to the extracted KSP `submission_dataset` folder.
# The dataset is distributed separately and is not committed to this repo.
# If unset, PRAHARI looks for <repo>/dataset.
PRAHARI_DATASET_DIR=/absolute/path/to/submission_dataset
